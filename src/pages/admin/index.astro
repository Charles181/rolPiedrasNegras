---
import AdminLayout from "../../layouts/AdminLayout.astro";
import { db, User, Campaign, Event, Post, count } from "astro:db";

const userCount = (await db.select({ value: count() }).from(User))[0].value;
const campaignCount = (await db.select({ value: count() }).from(Campaign))[0]
    .value;
const eventCount = (await db.select({ value: count() }).from(Event))[0].value;
const postCount = (await db.select({ value: count() }).from(Post))[0].value;

const stats = [
    {
        label: "Usuarios",
        value: userCount,
        icon: "👥",
        color: "from-blue-500 to-blue-600",
    },
    {
        label: "Campañas",
        value: campaignCount,
        icon: "🎯",
        color: "from-purple-500 to-purple-600",
    },
    {
        label: "Eventos",
        value: eventCount,
        icon: "📅",
        color: "from-green-500 to-green-600",
    },
    {
        label: "Posts",
        value: postCount,
        icon: "📝",
        color: "from-orange-500 to-orange-600",
    },
];
---

<AdminLayout title="Dashboard">
    <div class="space-y-6">
        <!-- Page Header -->
        <div>
            <h1 class="text-3xl font-bold text-white">Dashboard</h1>
            <p class="text-slate-400 mt-1">
                Bienvenido al panel de administración
            </p>
        </div>

        <!-- Stats Grid -->
        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4">
            {
                stats.map((stat) => (
                    <div class="bg-slate-800 rounded-xl border border-slate-700 p-6 hover:border-slate-600 transition-colors">
                        <div class="flex items-center justify-between">
                            <div>
                                <p class="text-slate-400 text-sm font-medium">
                                    {stat.label}
                                </p>
                                <p class="text-3xl font-bold text-white mt-1">
                                    {stat.value}
                                </p>
                            </div>
                            <div
                                class={`w-12 h-12 rounded-lg bg-gradient-to-br ${stat.color} flex items-center justify-center text-2xl`}
                            >
                                {stat.icon}
                            </div>
                        </div>
                    </div>
                ))
            }
        </div>

        <!-- Quick Actions -->
        <div class="bg-slate-800 rounded-xl border border-slate-700 p-6">
            <h2 class="text-xl font-bold text-white mb-4">Acciones Rápidas</h2>
            <div class="grid grid-cols-2 md:grid-cols-4 gap-4">
                <a
                    href="/admin/users"
                    class="flex flex-col items-center gap-2 p-4 bg-slate-700/50 rounded-lg hover:bg-slate-700 transition-colors"
                >
                    <span class="text-3xl">👤</span>
                    <span class="text-sm text-slate-300">Nuevo Usuario</span>
                </a>
                <a
                    href="/admin/campaigns"
                    class="flex flex-col items-center gap-2 p-4 bg-slate-700/50 rounded-lg hover:bg-slate-700 transition-colors"
                >
                    <span class="text-3xl">🎯</span>
                    <span class="text-sm text-slate-300">Nueva Campaña</span>
                </a>
                <a
                    href="/admin/events"
                    class="flex flex-col items-center gap-2 p-4 bg-slate-700/50 rounded-lg hover:bg-slate-700 transition-colors"
                >
                    <span class="text-3xl">📅</span>
                    <span class="text-sm text-slate-300">Nuevo Evento</span>
                </a>
                <a
                    href="/admin/posts"
                    class="flex flex-col items-center gap-2 p-4 bg-slate-700/50 rounded-lg hover:bg-slate-700 transition-colors"
                >
                    <span class="text-3xl">📝</span>
                    <span class="text-sm text-slate-300">Nuevo Post</span>
                </a>
            </div>
        </div>

        <!-- Recent Activity Placeholder -->
        <div class="bg-slate-800 rounded-xl border border-slate-700 p-6">
            <h2 class="text-xl font-bold text-white mb-4">
                Actividad Reciente
            </h2>
            <div class="text-center py-8">
                <div class="text-4xl mb-2">📋</div>
                <p class="text-slate-400">No hay actividad reciente</p>
            </div>
        </div>
    </div>
</AdminLayout>
